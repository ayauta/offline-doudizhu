# Privacy

单机斗地主不建立账号、不收集个人数据，也不包含广告、分析、遥测、追踪、
远程配置或业务网络接口。牌局、允许的设置和未来恢复快照只保留在用户设备。

## 网络边界

普通 Web 首次打开可以从部署者选择的同源位置取得静态应用。安装或更新
PWA 时，生成的 service worker 只可以取得构建时审查过的固定同源静态文件。
安装完成后，完整版本必须离线启动和运行。

官方公开 Web 预览由 GitHub Pages 静态托管。Pages 只是传送同源构建文件，
不是游戏后端；应用不向它或其他服务上传牌局、设置或设备信息。

应用源码禁止 `fetch`、XMLHttpRequest、WebSocket、EventSource、Beacon、
外部字体/图片/脚本、远程日志和任何业务 API。不得把生成 worker 的静态安装
例外扩大成通用网络能力。

## 本地数据

未来持久化仅允许：一个带版本和校验的未完成牌局、一个恢复槽以及获准的
显示/声音/难度设置。不得保存牌局历史、胜率、身份、设备标识或参与度数据。
当前迁移切片不实现持久化。

## 仓库数据

不得提交托管凭据、Android 签名材料、API 密钥、令牌、Cookie、会话、私人
邮箱、真实姓名、电话号码、证书、keystore、设备标识或其他秘密/PII。此政策
同时适用于源码、测试夹具、日志、截图、构建产物和 Git 作者信息。

发现问题时请按 `SECURITY.md` 私下报告，不要在公开 issue 中附带敏感数据。
